
data_sources:
  main:
    url: <%= ENV["DATABASE_URL"] %>
  production:
    url: <%= ENV["PRODUCTION_FOLLOWER_DB"] %>
  prod-primary:
    url: <%= ENV["PRODUCTION_PRIMARY_DB"] %>

from_email: alerts@happyscribe.com

check_schedules:
  - "5 minutes"